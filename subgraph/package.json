{
  "name": "subgraph",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "gen-config": "node scripts/gen-config.mjs",
    "codegen": "npm run gen-config && graph codegen",
    "build": "npm run codegen && graph build",
    "deploy": "npm run build && graph deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.68.0",
    "@graphprotocol/graph-ts": "^0.35.1"
  }
}
