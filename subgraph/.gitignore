node_modules
build
generated

# NOTE: src/config.ts is committed as an empty stub. scripts/gen-config.mjs
# rewrites it from the RELAYER_ADDRESSES env var before each deploy. Don't
# commit the regenerated version — `git checkout subgraph/src/config.ts`
# after `npm run build` if you accidentally stage it.
